import { RandomValuePipe } from './random-value-pipe';

describe('RandomValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomValuePipe();
    expect(pipe).toBeTruthy();
  });
});
